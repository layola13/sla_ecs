@import "bevy_readme_parity_support.sla"

fn main() -> i32 { return bevy_style_parity_demo(); }

@test "bevy readme parity demo covers core ecs flow"() {
    if bevy_style_parity_demo() != 17 { panic(9310); };
}
