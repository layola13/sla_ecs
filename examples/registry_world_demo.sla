@import "registry_world_support.sla"

fn main() -> i32 { return bevy_style_registry_world_demo(); }

@test "registry world demo runs arbitrary component index"() {
    if bevy_style_registry_world_demo() != 5 { panic(8804); };
}
